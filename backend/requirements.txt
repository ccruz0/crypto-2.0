# FastAPI and ASGI server
fastapi==0.141.1
uvicorn[standard]==0.52.4
gunicorn==26.1.0

# Database
sqlalchemy==2.0.36
alembic==1.18.5
psycopg2-binary==2.9.10

# Authentication and Security
python-jose[cryptography]>=3.3.0,<4.0.0
# cryptography: declarado explicitamente el 27-ago-2026. github_app_auth.py
# lo importa en cabecera (lineas 19-21) SIN proteccion, pero solo llegaba
# como extra de python-jose[cryptography]. Si esa resolucion cambiase, el
# modulo reventaria al importarse.
cryptography==50.0.1
passlib[bcrypt]==1.7.4
python-multipart>=0.0.32

# Environment variables
python-dotenv==1.0.1

# HTTP client
httpx==0.28.1
# AWS (SSM for /api/agent/run-atp-command)
boto3>=1.34.0
# Jarvis LangGraph MVP orchestration
langgraph>=0.2.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
requests==2.32.3
# aiohttp: pin explicito. Hasta el 27-ago-2026 estaba clavado en 3.8.x de forma
# implicita por alpaca-trade-api; al eliminar ese paquete, pip resolvia 3.14.3
# por su cuenta. Se fija a proposito para que quitar alpaca mueva UNA sola
# dependencia (websockets) y no dos: si el despliegue falla, se sabe por que.
# Subirlo es un paso aparte, con su propia verificacion, y levanta la
# limitacion de seguridad que la serie 3.8 arrastraba.
# Se fija 3.8.3 y no 3.8.2: pip marca 3.8.2 como *yanked* por sus propios
# autores (limite superior de multidict demasiado restrictivo, corregido
# justo en 3.8.3). Produccion corre hoy la version retirada.
aiohttp==3.8.3
# websockets: sin pin a proposito. Ningun modulo del backend lo importa
# (verificado 27-ago-2026); lo arrastra uvicorn[standard] para servir /ws/prices
# y es uvicorn quien debe decidir su version. El pin ==10.4 existia por
# alpaca-trade-api, ya eliminado, y rompio el build el 17-ago-2026 al chocar
# con uvicorn 0.51 (issue #490).

# Data validation
pydantic==2.9.2
pydantic-settings==2.15.0

# Data analysis
numpy==2.1.1
# pandas nunca estuvo declarado: lo arrastraba alpaca-trade-api, que exigia
# pandas>=0.18.1 y era su UNICO proveedor. Al eliminar alpaca el 27-ago-2026
# (ef8b3a41, #576) pandas desaparecio de la imagen y signal_monitor se quedo
# sin datos de precio para TODOS los simbolos durante 66 minutos.
#
# CORRECCION: el commit f59831d0 (#579) atribuyo la causa al bump de uvicorn
# (#547). Es falso. Verificado ejecutando las imagenes de ECR en el host:
#   a39959f4 (anterior)         -> pandas 3.0.5
#   ef8b3a41 (#576, sin alpaca) -> ModuleNotFoundError
#   a6e25060 (#547, uvicorn)    -> ModuleNotFoundError (heredado)
# El bump de uvicorn no mueve ningun paquete transitivo. Aquel diagnostico
# comparo a39959f4 contra a6e25060, una ventana que contiene ambos commits.
#
# Va pinneado porque esta en la ruta critica del bot y no puede depender de
# que otro paquete lo arrastre.
pandas==3.0.5
# python-dateutil: declarado explicitamente el 27-ago-2026. Lo importan
# routes_orders.py:2804 y :2842, y expected_take_profit.py:3305 (superficie
# TP). Solo llegaba de forma transitiva via pandas y celery: la misma clase
# de dependencia fantasma que dejo al bot ciego ese mismo dia.
python-dateutil==2.9.0.post0
pytz==2024.2

# Auto ML entry model runtime load + offline retrain (keep in sync with scripts/requirements-auto-ml.txt)
scikit-learn>=1.3.0,<1.6
joblib>=1.3.0,<1.5

# Trading APIs
ccxt==4.2.65

# Google Ads / protobuf (jarvis google_ads_mutations boot import)
protobuf>=5.29.0

# Observability (Phase 7)
prometheus-client==0.20.0

# Brief calendar (ICS + RRULE expansion)
icalendar>=6.0.0,<8
recurring-ical-events>=3.0.0,<4

# Brief Telegram user-session reader (MTProto)
telethon>=1.36.0,<2

# Background tasks
celery==5.4.0
redis==8.1.0

# Testing (NOT installed in production Docker image)
# pytest==7.4.3
# pytest-asyncio==0.21.1
# Note: httpx is installed above in HTTP client section

# Development (NOT installed in production Docker image)
# black==23.11.0
# isort==5.12.0
# flake8==6.1.0


# One-shot: merge to main → recreate DOGE_USD SL+TP for naked short.
# run=1: cancelled SLs then wallet_empty_short (available=0 on loans).
# run=2: wallet gate fixed; TP 0.0692 coerced to "0.07" (price_decimals=2) → INVALID_TRIGGER_PRICE.
# run=3: quote_decimals/tick for price_decimals + never change TP value in format variations
#        + market-relative short levels in ops script.
# Remove this file after the ops run succeeds (same pattern as #360/#391).
run=3

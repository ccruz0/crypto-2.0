# Evidence: PR2 — Sync truth: missing ≠ canceled

**Status:** Implemented.

## Goal

Orders missing from open_orders are not marked CANCELLED unless order_history (or cancel API) confirms terminal status.

## Commands

From repo root:

```bash
cd "$(git rev-parse --show-toplevel)"

python3 -m pytest backend/tests/test_sync_missing_not_canceled.py -v --tb=short
```

## Evidence capture

```bash
cd "$(git rev-parse --show-toplevel)"
mkdir -p docs/audits/openclaw/EVIDENCE/logs
python3 -m pytest backend/tests/test_sync_missing_not_canceled.py -v --tb=short 2>&1 | tee docs/audits/openclaw/EVIDENCE/logs/pr-02_$(date +%Y%m%d_%H%M%S).txt
```

## Expected outcome

- All tests in test_sync_missing_not_canceled.py pass (e.g. missing + resolve None → status unchanged; resolve CANCELLED → status CANCELLED).

## Rollback check

If sync logic is changed to mark missing-as-canceled without history, test_missing_order_with_resolve_none_stays_unchanged will fail.

# Retired: do not attach Bedrock IAM to the jarvis-lab-bedrock *user* from CI.
# LAB Bedrock must use an EC2 instance role (see scripts/aws/create_atp_lab_ec2_role.sh).
# This workflow no longer uses AWS_ACCESS_KEY_ID.
name: Attach Jarvis LAB Bedrock IAM policy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  attach:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: |
          echo "RETIRED: do not attach inline IAM user policies from GitHub Actions."
          echo "LAB Bedrock: instance role, not jarvis-lab-bedrock static keys."
          echo "Use AWS_PROFILE=carlos-sso ./scripts/aws/create_atp_lab_ec2_role.sh"
          exit 1

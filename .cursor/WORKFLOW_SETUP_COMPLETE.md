# Strict Watchlist Audit Workflow - Setup Complete

## ✅ Workflow Configuration Created

The "Strict Watchlist Audit" workflow has been configured and saved to:

**Location:** `.cursor/workflows.json`

## Workflow Details

- **Name:** Strict Watchlist Audit
- **Trigger Keywords:**
  - audit watchlist
  - watchlist audit
  - strict audit
  - verify watchlist
  - check signals
- **Auto-trigger:** Enabled
- **Prompt:** Full autonomous audit workflow (see workflows.json for complete prompt)

## Next Steps

The workflow configuration file has been created. To activate it in Cursor:

1. **Option 1 (Automatic):** Restart Cursor - it may automatically detect the workflow file
2. **Option 2 (Manual):** 
   - Open Cursor Settings → Workflows
   - Click "Import" or "Add Workflow"
   - Select `.cursor/workflows.json` or manually create the workflow using the configuration

## Verification

To verify the workflow is active:
- Open Cursor Settings → Workflows
- Look for "Strict Watchlist Audit" in the workflows list
- Test by typing one of the trigger keywords in a chat

## Files Created

- `.cursor/workflows.json` - Workflow configuration
- `.cursor/create_workflow.py` - Script to recreate/update the workflow


